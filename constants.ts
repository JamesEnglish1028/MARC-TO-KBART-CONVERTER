import { KbartRow } from './types';

export const APP_KBART_HEADERS: (keyof KbartRow)[] = [
    'publication_title',
    'print_identifier',
    'online_identifier',
    'publisher_name',
    'publication_date',
    'title_url',
];
